spring:
  application:
    name: sport-life
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db-changelog-master.xml

server:
  port: 49182
  address: 0.0.0.0
  servlet:
    context-path: /api
  error:
    include-message: always
